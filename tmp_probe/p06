   tests.append({
                    'name': test_name,
                    'dir': test_dir,
                    **metrics
                })

    # Count results
    pass_count = sum(1 for t in tests if t['status'] == 'PASS')
    fail_count = sum(1 for t in tests if t['status'] == 'FAIL')
    total = len(tests)

 