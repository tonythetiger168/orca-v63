join(regression_dir, 'report.html')
    with open(report_file, 'w') as f:
        f.write(html)

    print(f"Report generated: {report_file}")

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print("Usage: generate_report.py <regression_directory>")
        sys.exit(1)

    generate_html(sys.argv[1])
